// Day 1: The Tyranny of the Rocket Equation
const fs = require('fs');

let modules = fs.readFileSync('1_input.txt', 'utf-8').split('\n');
console.log(modules);

for (let module of modules) {
  // calculate fuel req
}